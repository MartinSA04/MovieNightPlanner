# Shared Config

This package holds shared configuration artifacts for the workspace. Right now it provides the base TypeScript config used by the app and shared packages. It can later expand to include shared ESLint, Tailwind, or build presets.

